tooltim means add title //The value of the title attribute will be displayed as a tooltip when you mouse over the element:

 <a href="" target="_blank"></a> // opens a web page in new tab
 select is used for making drop down list

 Empty elements do not have an end tag! eg: br tag 

 ctrl + u to see web source
 An HTML element is defined by a start tag, some content, and an end tag.

some elements will display correctly, even if you forget the end tag:
HTML attributes provide additional information about HTML elements.

The title attribute defines some extra information about an element

<pre> tag preserve both line breaks and spaces

<strong> - Important text

The HTML <mark> element defines text that should be marked or highlighted:

To display black, set all color parameters to 0, like this: rgb(0, 0, 0).

To display white, set all color parameters to 255, like this: rgb(255, 255, 255).

A hexadecimal color is specified with: #RRGGBB, where the RR (red), GG (green) and BB (blue)

The word cascading means that a style applied to a parent element will also apply to all children elements within the parent.

The target attribute specifies where to open the linked document
_self - Default. Opens the document in the same window/tab as it was clicked
_blank - Opens the document in a new window or tab
_parent - Opens the document in the parent frame
_top - Opens the document in the full body of the window


An unvisited link is underlined and blue
A visited link is underlined and purple
An active link is underlined and red

for clickable images use it inside the anchor tag

A favicon is a small image displayed next to the page title in the browser tab.


If you have a <div> element that is not 100% wide, and you want to center-align it, set the CSS margin property to auto

inlin-block to align in one line;

overflow auto if text goesout off box;

picture tag show differnet images depend on browser size // works as a switch case

Viewport is the browser window size. 1vw = 1% of viewport width. If the viewport is 50cm wide, 1vw is 0.5cm.

if we have to display file we can use object to display file on browser




SVG graphics are scalable, and do not lose any quality if they are zoomed or resized: